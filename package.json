{
  "name": "sql-it",
  "displayName": "SQL it!",
  "description": "Turn a list of strings into a inline, comma-seperated, sql ready string",
  "version": "1.0.11",
  "publisher": "schababerledigital",
  "engines": {
    "vscode": "^1.96.0"
  },
  "icon": "assets/logo.png",
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "repository": "https://github.com/Schababerle-Digital/sql-it-vs-code-extension.git",
  "contributes": {
    "commands": [{
      "command": "sql-it.formatStrings",
      "title": "SQL it!"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.96.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "eslint": "^9.16.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  }
}
